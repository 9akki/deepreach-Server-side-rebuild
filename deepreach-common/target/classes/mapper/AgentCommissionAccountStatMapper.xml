<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deepreach.common.core.mapper.AgentCommissionAccountStatMapper">

    <select id="selectCommissionByUserIds" parameterType="map" resultType="map">
        SELECT
            agent_user_id AS userId,
            COALESCE(total_commission, 0) AS totalCommission,
            COALESCE(settled_commission, 0) AS settledCommission
        FROM dr_agent_commission_account
        WHERE agent_user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </select>

</mapper>
