### 云电脑参数查询接口测试

### 1. 获取指定用户的云电脑参数
GET http://localhost:8080/api/cloud-computer/parameter/1
Content-Type: application/json
Authorization: Bearer your-token-here

### 2. 获取当前用户的云电脑参数
GET http://localhost:8080/api/cloud-computer/parameter/current
Content-Type: application/json
Authorization: Bearer your-token-here

### 3. 测试不存在的用户
GET http://localhost:8080/api/cloud-computer/parameter/999999
Content-Type: application/json
Authorization: Bearer your-token-here

### 4. 测试未分配云电脑的用户（假设用户ID为2）
GET http://localhost:8080/api/cloud-computer/parameter/2
Content-Type: application/json
Authorization: Bearer your-token-here

### 5. 测试未授权访问（无token）
GET http://localhost:8080/api/cloud-computer/parameter/1
Content-Type: application/json

### 数据库表结构参考：
-- t_cloud_user 表
CREATE TABLE t_cloud_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    client_name VARCHAR(100) NOT NULL COMMENT '客户端名称，与sys_user.username对应',
    end_user_id VARCHAR(100) NOT NULL COMMENT '最终用户ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    remark VARCHAR(500)
);

-- t_user_computer 表
CREATE TABLE t_user_computer (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    end_user_id VARCHAR(100) NOT NULL COMMENT '最终用户ID',
    computer_id VARCHAR(100) NOT NULL COMMENT '云电脑ID',
    assign_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    remark VARCHAR(500)
);

-- t_computer 表
CREATE TABLE t_computer (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    computer_id VARCHAR(100) NOT NULL COMMENT '云电脑ID',
    office_site_id VARCHAR(200) NOT NULL COMMENT '办公站点ID',
    computer_name VARCHAR(100),
    status VARCHAR(10) DEFAULT '0' COMMENT '状态：0-正常，1-停用',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    remark VARCHAR(500)
);

### 测试数据插入脚本：
-- 插入测试用户（假设在sys_user表中已存在用户名为testuser的用户）
INSERT INTO t_cloud_user (client_name, end_user_id, remark) VALUES
('testuser', 'end-user-001', '测试用户云电脑配置');

-- 插入云电脑配置
INSERT INTO t_computer (computer_id, office_site_id, computer_name, status, remark) VALUES
('comp-001', 'us-west-1+dir-5588339126', '测试云电脑', '0', '测试用云电脑');

-- 插入用户云电脑关联
INSERT INTO t_user_computer (end_user_id, computer_id, remark) VALUES
('end-user-001', 'comp-001', '测试用户云电脑关联');